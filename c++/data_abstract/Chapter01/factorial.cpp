int factorial(int n)// ------------------------------------------------------// Computes the factorial of an integer.// Precondition: n >= 0.// Postcondition: Returns n * (n-1)*...*1, if n > 0;// returns 1 if n = 0.// ------------------------------------------------------{   int fact = 1;   for (int i = n; i > 1; --i)      fact *= i;   return fact;} // end factorial